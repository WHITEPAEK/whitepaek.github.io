---
import Layout from "@/layouts/Layout.astro";
import Diary from "@/components/Diary/Diary";
import {
  type CollectionEntry,
  type CollectionKey,
  getCollection,
} from "astro:content";

const pageTitle = "다이어리";
const pageDesc = "정리되지 않은 생각의 조각들을 짧게 기록하는 공간입니다.";

const posts: CollectionEntry<CollectionKey>[] = (
  await getCollection("diary", ({ data }: CollectionEntry<CollectionKey>) => {
    return import.meta.env.PROD ? data.draft !== true : true;
  })
).sort(
  (a: any, b: any) =>
    new Date(b.data.pubDate).getTime() - new Date(a.data.pubDate).getTime(),
);
---

<Layout title={pageTitle}>
  <Diary title={pageTitle} description={pageDesc} posts={posts} />
</Layout>
